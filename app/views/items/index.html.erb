<div class="well">
  <div class="text-center row divider" >
    <h1 class="green-font mousiness" >All Items</h1>
  </div>
  <div class="items">
    <ul>
      <% @items.each do |item| %>
        <li><%= item.title %></li>
        <li><%= item.formatted_price %></li>
      <% end %>
    </ul>
  </div>
</div>
